DATABASE_URL=postgresql://admin_casadapaz:changeme@localhost:5433/casadapaz_db
JWT_SECRET=change-me-in-production
PORT=3000
CORS_ORIGIN=http://localhost:5173
PIX_WEBHOOK_SECRET=pix-dev-secret
N8N_WEBHOOK_SECRET=n8n-dev-secret
N8N_URL=http://localhost:5678
CHATWOOT_URL=http://localhost:3001
AI_SERVICE_URL=http://localhost:8000

# Cloudflare Turnstile (portal — omitir em dev local para desabilitar)
# TURNSTILE_SITE_KEY=   # pública — exposta em GET /api/public/portal-config
# TURNSTILE_SECRET_KEY=

# Stripe deprecated — usar Asaas (ADR-009)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# Asaas PSP (sandbox por padrão — produção só com confirmação explícita)
ASAAS_ENV=sandbox
ASAAS_API_KEY=
ASAAS_WEBHOOK_TOKEN=asaas-dev-webhook-token
# ASAAS_WALLET_ID=
# ASAAS_API_URL=   # opcional override; default sandbox/prod conforme ASAAS_ENV

# CF_ACCOUNT_ID=
# CF_IMAGES_API_TOKEN=
