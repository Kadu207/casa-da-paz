# Plan — 033 Delegações

1. Schema + migration + seed funções
2. RBAC `delegacoes` + rotas Express
3. N8N workflow `tarefa_delegacao` + sync-alertas
4. FE menu + `DelegacoesPage`
5. Docs / memória / validadores
6. Deploy VPS sob confirmação
